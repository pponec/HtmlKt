# HtmlKt

Example of a live HTML page built using Kotlin/JS (without using Javascript directly. The button on the page replaces the original table content with a new table. 
The page works independently without a server. See the next screenshot.

![screen.png](/home/.pavel/Vyvoj/HtmlKt/docs/screen.png)